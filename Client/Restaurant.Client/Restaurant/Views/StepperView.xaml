<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Restaurant.Controls;assembly=Restaurant"
             xmlns:abstractions="clr-namespace:RoundedBoxView.Forms.Plugin.Abstractions;assembly=RoundedBoxView.Forms.Plugin.Abstractions"
             x:Class="Restaurant.Views.StepperView">
    <ContentView.Content>
        <StackLayout Orientation="Horizontal">
            <Button Image="minus" WidthRequest="30" HeightRequest="15" Clicked="MinusButton_OnClicked" BackgroundColor="Transparent"></Button>
            <Grid VerticalOptions="Center">
                <abstractions:RoundedBoxView BackgroundColor="White" 
                                             BorderColor="{StaticResource grayDark}" BorderThickness="1" 
                                             HeightRequest="30" WidthRequest="60"  
                                             CornerRadius="8" />
                <Label TextColor="Black"  x:Name="StepLabel" HorizontalOptions="Center" FontSize="16" VerticalOptions="Center"/>

            </Grid>
            <Button Image="plus" WidthRequest="30" HeightRequest="15" BackgroundColor="Transparent" Clicked="PlusButton_OnClicked"></Button>
        </StackLayout>
    </ContentView.Content>
</ContentView>